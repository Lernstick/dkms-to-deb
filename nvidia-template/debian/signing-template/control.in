Source: ${PACKAGE_NAME}-${KERNEL_VERSION}-signed
Section: admin
Priority: optional
Maintainer: Lernstick Kernel Module Team <info@lernstick.ch>
Standards-Version: 4.3.0
Build-Depends: debhelper (>= 10.1~),
 ${PACKAGE_NAME}-${KERNEL_VERSION}-unsigned (= ${MODULE_VERSION}${DEB_VERSION})

Package: ${PACKAGE_NAME}-${KERNEL_VERSION}-signed
Depends: linux-kbuild-${KBUILD_VERSION}, binutils, depmod-assemble, nvidia-driver (= ${PACKAGE_VERSION}) 
Provides: ${PACKAGE_NAME}
Architecture: ${DEBIAN_BUILD_ARCH}
Built-Using: ${PACKAGE_NAME}-${KERNEL_VERSION} (= ${MODULE_VERSION}${DEB_VERSION})
Description: Singed version of ${DEBIAN_PACKAGE} as binary package.
